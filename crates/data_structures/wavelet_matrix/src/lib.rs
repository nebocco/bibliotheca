use bit_vector::BitVector;

struct WaveletMatrix();

impl WaveletMatrix {
    fn new() -> Self {
        WaveletMatrix{}
    }
}

#[cfg(test)]
mod tests {
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}
