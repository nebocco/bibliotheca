use crate::algorithms::dp::Monge;

fn smawk<T: Monge>(table: &T) -> Vec<usize> {
	// let v = table.size();
	vec![0]
}

#[cfg(test)]
mod tests {
    // TODO: make tests
    #[test]
    fn it_works() {
        assert_eq!(2 + 2, 4);
    }
}